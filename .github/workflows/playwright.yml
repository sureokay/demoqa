name: Playwright Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 'lts/*'

      - name: Install dependencies
        run: npm ci

      - name: Install XVFB and necessary libraries for Playwright
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libxcomposite1 libxdamage1 libxi6 libxtst6 libnss3 libasound2 libatk1.0-0 libcups2 libxss1 libxrandr2 libpangocairo-1.0-0 libgtk-3-0

      - name: Install Playwright Browsers
        run: npx playwright install

      - name: Set up Environment Variables
        env:
          APP_USERNAME: ${{ secrets.APP_USERNAME }}
          APP_PASSWORD: ${{ secrets.APP_PASSWORD }}
        run: echo "APP_USERNAME=$APP_USERNAME" >> .env && echo "APP_PASSWORD=$APP_PASSWORD" >> .env

      - name: Run Playwright tests
        run: npx playwright test

      - name: Upload Playwright report
        uses: actions/upload-artifact@v2
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30